# dependências
node_modules/
.pnpm-store/

# build
dist/
.next/
out/

# segredos — nunca comitar
.env
.env.local
.env.*.local

# dados locais com PII (telefone de cliente) — o FileTokenStore de dev
# grava aqui; nunca sobe pro Git, é só um placeholder até existir Supabase
engine/data/

# grafo de conhecimento (gerado, reconstrua com /graphify)
graphify-out/

# sistema
.DS_Store
*.log

# cache de build do TypeScript
*.tsbuildinfo
